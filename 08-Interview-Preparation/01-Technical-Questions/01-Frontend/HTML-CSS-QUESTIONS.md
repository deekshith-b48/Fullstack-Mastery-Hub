# HTML & CSS Interview Questions

**150+ Questions organized by difficulty level for top tech company interviews**

---

## 📚 Basics (50 Questions)

1. What is HTML and what does it stand for?
2. Explain the difference between HTML and XHTML.
3. What is the purpose of the `<!DOCTYPE>` declaration?
4. Explain the difference between block-level and inline elements.
5. What are semantic HTML elements? Provide examples.
6. What is the difference between `<div>` and `<span>`?
7. Explain the purpose of HTML5 semantic tags (`<header>`, `<nav>`, `<section>`, `<article>`, `<footer>`).
8. What is the difference between `<ul>`, `<ol>`, and `<dl>`?
9. Explain the purpose of the `alt` attribute in images.
10. What is the difference between `<strong>` and `<b>` tags?
11. What is the difference between `<em>` and `<i>` tags?
12. Explain the purpose of HTML forms and their main elements.
13. What is the difference between `GET` and `POST` methods in forms?
14. Explain the purpose of the `name` and `id` attributes.
15. What are HTML5 form input types?
16. What is CSS and how does it work?
17. Explain the three ways to include CSS in HTML.
18. What is the CSS syntax?
19. Explain CSS selectors: element, class, and ID selectors.
20. What is the difference between class and ID selectors?
21. Explain descendant selectors in CSS.
22. What is the CSS box model?
23. Explain padding, margin, and border in CSS.
24. What is the difference between `margin` and `padding`?
25. Explain the `display` property and its common values.
26. What is the difference between `display: block` and `display: inline`?
27. Explain `display: inline-block`.
28. What is the `position` property in CSS?
29. Explain `position: relative`, `absolute`, `fixed`, and `static`.
30. What is the `z-index` property and when is it used?
31. Explain CSS colors: named colors, hex, RGB, and RGBA.
32. What is the difference between `px`, `em`, and `rem` units?
33. Explain CSS font properties: `font-family`, `font-size`, `font-weight`, `font-style`.
34. What is the `text-align` property used for?
35. Explain `text-decoration` and its values.
36. What is the `line-height` property?
37. Explain CSS background properties.
38. What is the `border` property and its shorthand?
39. Explain the `border-radius` property.
40. What is the `opacity` property?
41. Explain CSS pseudo-classes like `:hover`, `:active`, `:focus`.
42. What is the `:first-child` pseudo-class?
43. Explain CSS pseudo-elements `::before` and `::after`.
44. What is the `float` property and how is it used?
45. Explain the `clear` property.
46. What is CSS specificity and how is it calculated?
47. Explain the cascade in CSS.
48. What is the `!important` rule and when should it be used?
49. Explain CSS inheritance.
50. What is the difference between `inline`, `internal`, and `external` CSS?

---

## 🎯 Intermediate (50 Questions)

51. Explain CSS Flexbox and its main properties.
52. What is `flex-direction` and what are its values?
53. Explain `justify-content` and `align-items` in Flexbox.
54. What is `flex-wrap` and when is it used?
55. Explain `flex-grow`, `flex-shrink`, and `flex-basis`.
56. What is the difference between `align-items` and `align-self`?
57. Explain CSS Grid and its main properties.
58. What is `grid-template-columns` and `grid-template-rows`?
59. Explain `grid-gap` and its properties.
60. What is `grid-template-areas` and how is it used?
61. Explain `grid-column` and `grid-row` properties.
62. What is the difference between `grid-column-start` and `grid-column-end`?
63. When would you use Flexbox vs CSS Grid?
64. Explain CSS variables (custom properties).
65. How do you define and use CSS variables?
66. What is the difference between `:root` and `html` selector for CSS variables?
67. Explain CSS media queries and responsive design.
68. What are common breakpoints for responsive design?
69. Explain `min-width` and `max-width` in media queries.
70. What is mobile-first design approach?
71. Explain CSS transitions.
72. What properties can be transitioned in CSS?
73. Explain `transition-property`, `transition-duration`, `transition-timing-function`, and `transition-delay`.
74. What is CSS animation and how does it differ from transitions?
75. Explain `@keyframes` rule.
76. What is `animation-name`, `animation-duration`, and `animation-iteration-count`?
77. Explain CSS transforms: `translate`, `rotate`, `scale`, and `skew`.
78. What is the difference between `transform` and `transition`?
79. Explain CSS `box-sizing` property.
80. What is the difference between `box-sizing: content-box` and `box-sizing: border-box`?
81. Explain CSS `overflow` property and its values.
82. What is `text-overflow` and when is it used?
83. Explain CSS `white-space` property.
84. What is `word-wrap` and `word-break`?
85. Explain CSS `vertical-align` property.
86. What is the difference between `visibility: hidden` and `display: none`?
87. Explain CSS `opacity` vs `rgba()` for transparency.
88. What is CSS `filter` property and its common uses?
89. Explain CSS `clip-path` property.
90. What is CSS `object-fit` property and when is it used?
91. Explain CSS `background-size`, `background-position`, and `background-repeat`.
92. What is CSS `background-attachment` property?
93. Explain CSS `linear-gradient` and `radial-gradient`.
94. What is CSS `box-shadow` and how is it used?
95. Explain CSS `text-shadow` property.
96. What is the difference between `margin: auto` and `text-align: center`?
97. Explain CSS `calc()` function.
98. What is CSS `clamp()` function?
99. Explain CSS `min()`, `max()`, and `clamp()` functions.
100. What is the difference between `em` and `rem` units?

---

## 🚀 Advanced (50+ Questions)

101. Explain CSS BEM (Block Element Modifier) methodology.
102. What are the benefits of using BEM?
103. Explain CSS Modules and how they work.
104. What is CSS-in-JS and its advantages/disadvantages?
105. Explain CSS preprocessors (SASS, LESS, Stylus).
106. What are SASS variables and how do they differ from CSS variables?
107. Explain SASS nesting and when to use it.
108. What are SASS mixins and how do you create them?
109. Explain SASS `@include` and `@extend` directives.
110. What are SASS functions and how are they used?
111. Explain CSS architecture patterns (OOCSS, SMACSS, Atomic CSS).
112. What is the difference between CSS reset and CSS normalize?
113. Explain critical CSS and how to implement it.
114. What is CSS containment and its benefits?
115. Explain CSS `will-change` property and performance implications.
116. What is CSS `contain` property and its values?
117. Explain CSS `aspect-ratio` property.
118. What is CSS `gap` property and its use cases?
119. Explain CSS `subgrid` feature.
120. What is CSS `container queries` and how do they work?
121. Explain CSS logical properties and values.
122. What are CSS custom properties with JavaScript?
123. Explain CSS `@supports` rule (feature queries).
124. What is CSS `@layer` and its purpose?
125. Explain CSS `@scope` rule.
126. What is CSS `@container` rule?
127. Explain CSS `@media` query features beyond width/height.
128. What is CSS `prefers-color-scheme` media query?
129. Explain CSS `prefers-reduced-motion` media query.
130. What is CSS `@page` rule and its use cases?
131. Explain CSS `@import` rule and its performance implications.
132. What is the difference between `@import` and `<link>` for CSS?
133. Explain CSS specificity calculation in detail.
134. What happens when specificity is equal?
135. Explain CSS selector performance and optimization.
136. What are CSS `:is()` and `:where()` pseudo-classes?
137. Explain CSS `:has()` pseudo-class (parent selector).
138. What is CSS `::backdrop` pseudo-element?
139. Explain CSS `::marker` pseudo-element.
140. What is CSS `::placeholder` pseudo-element?
141. Explain CSS `::selection` pseudo-element.
142. What is CSS `@font-face` rule and how is it used?
143. Explain web font loading strategies (font-display property).
144. What is CSS `font-display` property and its values?
145. Explain CSS `unicode-range` property.
146. What is CSS `font-variant` and `font-feature-settings`?
147. Explain CSS `@counter-style` rule.
148. What is CSS `list-style` and its properties?
149. Explain CSS `quotes` property.
150. What is CSS `content` property and its use cases?
151. Explain CSS `counter-reset` and `counter-increment`.
152. What is CSS `@namespace` rule?
153. Explain CSS `@charset` rule.
154. What is CSS `@viewport` rule?
155. Explain CSS `@document` rule and its deprecation.
156. What are CSS custom properties inheritance and scoping?
157. Explain CSS `var()` function fallback values.
158. What is CSS `env()` function?
159. Explain CSS `attr()` function.
160. What is CSS `url()` function and its use cases?
161. Explain CSS `linear-gradient()` function syntax.
162. What is CSS `radial-gradient()` function?
163. Explain CSS `conic-gradient()` function.
164. What is CSS `repeating-linear-gradient()`?
165. Explain CSS `image()` function.
166. What is CSS `element()` function?
167. Explain CSS `cross-fade()` function.
168. What is CSS `image-set()` function?
169. Explain CSS `min-resolution` and `max-resolution` media queries.
170. What is CSS `prefers-contrast` media query?
171. Explain CSS `prefers-reduced-data` media query.
172. What is CSS `@media (hover: hover)` query?
173. Explain CSS `@media (pointer: fine)` query.
174. What is CSS `@media (prefers-reduced-motion: reduce)`?
175. Explain CSS `@media (prefers-color-scheme: dark)`.

---

**Total: 175 Questions**

*Questions organized by difficulty level to help you prepare systematically for technical interviews at top tech companies.*
