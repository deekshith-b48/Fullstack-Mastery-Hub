# JavaScript Fundamentals Interview Questions

**150+ Questions organized by difficulty level for top tech company interviews**

---

## 📚 Basics (50 Questions)

1. What is JavaScript and what is it used for?
2. Explain the difference between JavaScript and Java.
3. What are the different ways to include JavaScript in HTML?
4. Explain JavaScript data types (primitive and non-primitive).
5. What is the difference between `null` and `undefined`?
6. Explain `typeof` operator and its return values.
7. What is the difference between `==` and `===`?
8. Explain type coercion in JavaScript.
9. What are JavaScript variables and how do you declare them?
10. Explain the difference between `var`, `let`, and `const`.
11. What is variable hoisting in JavaScript?
12. Explain the difference between function scope and block scope.
13. What is the Temporal Dead Zone (TDZ)?
14. Explain JavaScript functions and function declarations.
15. What is the difference between function declaration and function expression?
16. Explain arrow functions and their syntax.
17. What is the difference between arrow functions and regular functions?
18. Explain function parameters and arguments.
19. What are default parameters in functions?
20. Explain rest parameters (`...args`) in functions.
21. What are JavaScript operators (arithmetic, comparison, logical)?
22. Explain the ternary operator.
23. What is the difference between `&&` and `||` operators?
24. Explain the `!` (NOT) operator.
25. What are JavaScript conditionals: `if`, `else`, `else if`, `switch`?
26. Explain `switch` statement and `case` clauses.
27. What is the `break` statement and when is it used?
28. Explain JavaScript loops: `for`, `while`, `do-while`.
29. What is the difference between `for` and `for...in` loops?
30. Explain `for...of` loop and when to use it.
31. What are `break` and `continue` statements in loops?
32. Explain JavaScript arrays and how to create them.
33. What are array methods: `push`, `pop`, `shift`, `unshift`?
34. Explain `slice()` and `splice()` array methods.
35. What is the difference between `slice()` and `splice()`?
36. Explain `indexOf()` and `includes()` methods.
37. What are JavaScript objects and object literals?
38. Explain how to access object properties (dot notation vs bracket notation).
39. What is the difference between dot notation and bracket notation?
40. Explain how to add and delete object properties.
41. What are JavaScript strings and string methods?
42. Explain `toUpperCase()`, `toLowerCase()`, `trim()`, `split()`, `join()`.
43. What is template literals (backticks) in JavaScript?
44. Explain string interpolation in template literals.
45. What is the `this` keyword in JavaScript?
46. Explain `this` in different contexts (global, function, method, arrow function).
47. What is JavaScript `Math` object and its common methods?
48. Explain `Date` object and date methods.
49. What is `NaN` and how to check for it?
50. Explain `isNaN()` and `Number.isNaN()` methods.

---

## 🎯 Intermediate (50 Questions)

51. What are JavaScript closures and how do they work?
52. Explain closure scope chain.
53. What is a closure example with a counter?
54. Explain closure memory leaks and how to avoid them.
55. What is the difference between `call()`, `apply()`, and `bind()`?
56. Explain `call()` method with examples.
57. What is `apply()` method and when to use it?
58. Explain `bind()` method and its use cases.
59. What is the difference between `call()` and `apply()`?
60. Explain JavaScript `this` binding with `call()`, `apply()`, and `bind()`.
61. What are JavaScript promises and why are they used?
62. Explain promise states: pending, fulfilled, rejected.
63. What is `Promise.then()` and `Promise.catch()`?
64. Explain promise chaining.
65. What is `Promise.all()` and how does it work?
66. Explain `Promise.race()` method.
67. What is `Promise.allSettled()`?
68. Explain `Promise.any()` method.
69. What is `async/await` syntax?
70. Explain the difference between promises and async/await.
71. What is error handling with `try-catch` in async/await?
72. Explain JavaScript event loop and how it works.
73. What is the call stack in JavaScript?
74. Explain macro tasks and micro tasks.
75. What is the execution order of synchronous code, promises, and setTimeout?
76. Explain JavaScript `setTimeout()` and `setInterval()`.
77. What is `clearTimeout()` and `clearInterval()`?
78. Explain `requestAnimationFrame()` and its use.
79. What are JavaScript array methods: `map()`, `filter()`, `reduce()`?
80. Explain `map()` method with examples.
81. What is `filter()` method and how does it work?
82. Explain `reduce()` method and its use cases.
83. What are other array methods: `forEach()`, `find()`, `findIndex()`, `some()`, `every()`?
84. Explain `forEach()` vs `map()` difference.
85. What is `some()` and `every()` methods?
86. Explain `find()` and `findIndex()` methods.
87. What is array destructuring in JavaScript?
88. Explain object destructuring.
89. What is nested destructuring?
90. Explain default values in destructuring.
91. What is the spread operator (`...`) in JavaScript?
92. Explain spread operator with arrays.
93. What is spread operator with objects?
94. Explain rest operator (`...`) in function parameters.
95. What is the difference between spread and rest operators?
96. Explain JavaScript object methods: `Object.keys()`, `Object.values()`, `Object.entries()`.
97. What is `Object.assign()` method?
98. Explain `Object.freeze()` and `Object.seal()`.
99. What is the difference between `Object.freeze()` and `Object.seal()`?
100. Explain `Object.create()` method.

---

## 🚀 Advanced (50+ Questions)

101. What is JavaScript prototype and prototype chain?
102. Explain how prototype inheritance works.
103. What is the difference between `__proto__` and `prototype`?
104. Explain `Object.getPrototypeOf()` and `Object.setPrototypeOf()`.
105. What is constructor function and how does it work?
106. Explain ES6 classes and their syntax.
107. What is the difference between class and constructor function?
108. Explain `extends` keyword and inheritance in classes.
109. What is `super` keyword in classes?
110. Explain static methods and properties in classes.
111. What are getters and setters in JavaScript?
112. Explain `Object.defineProperty()` method.
113. What is `Object.defineProperties()`?
114. Explain property descriptors: `configurable`, `enumerable`, `writable`, `value`.
115. What are getter and setter descriptors?
116. Explain JavaScript `Symbol` primitive type.
117. What is `Symbol()` and `Symbol.for()`?
118. Explain well-known symbols in JavaScript.
119. What is `WeakMap` and when to use it?
120. Explain `WeakSet` and its use cases.
121. What is the difference between `Map` and `WeakMap`?
122. Explain `Set` and `WeakSet` difference.
123. What is JavaScript `Proxy` and how does it work?
124. Explain proxy traps: `get`, `set`, `has`, `deleteProperty`.
125. What is `Reflect` API and its methods?
126. Explain the relationship between `Proxy` and `Reflect`.
127. What are generator functions and `yield` keyword?
128. Explain generator function syntax and usage.
129. What is `yield*` in generator functions?
130. Explain iterators and iterables in JavaScript.
131. What is the `Symbol.iterator` method?
132. Explain `for...of` loop with iterables.
133. What is the difference between iterable and iterator?
134. Explain JavaScript modules: ES6 modules vs CommonJS.
135. What is `export` and `import` in ES6 modules?
136. Explain default exports vs named exports.
137. What is `export default` vs `export`?
138. Explain dynamic imports with `import()`.
139. What is module scope and how does it work?
140. Explain JavaScript error types: `Error`, `SyntaxError`, `TypeError`, `ReferenceError`.
141. What is `throw` statement and how to use it?
142. Explain `try-catch-finally` blocks.
143. What is custom error classes?
144. Explain `Error.stack` property.
145. What is JavaScript `RegExp` and regular expressions?
146. Explain regex flags: `g`, `i`, `m`.
147. What are common regex patterns and metacharacters?
148. Explain `test()` and `exec()` methods.
149. What is `match()`, `matchAll()`, `replace()`, `search()` methods?
150. Explain JavaScript `JSON` object and methods.
151. What is `JSON.stringify()` and its parameters?
152. Explain `JSON.parse()` method.
153. What is `replacer` and `reviver` in JSON methods?
154. Explain JavaScript `fetch()` API.
155. What is the difference between `fetch()` and `XMLHttpRequest`?
156. Explain `fetch()` with `async/await`.
157. What is `Response` object and its methods?
158. Explain `Request` object and its properties.
159. What is `Headers` API in fetch?
160. Explain CORS and how it works with fetch.
161. What is JavaScript `localStorage` and `sessionStorage`?
162. Explain the difference between `localStorage` and `sessionStorage`.
163. What are `localStorage` methods: `setItem()`, `getItem()`, `removeItem()`, `clear()`?
164. Explain Web Storage API limitations.
165. What is JavaScript `cookie` manipulation?
166. Explain `document.cookie` property.
167. What is the difference between cookies and localStorage?
168. Explain JavaScript `FormData` API.
169. What is `FileReader` API?
170. Explain `Blob` and `File` objects.
171. What is `URL` and `URLSearchParams` API?
172. Explain `URL.createObjectURL()` and `URL.revokeObjectURL()`.
173. What is JavaScript `Intl` API for internationalization?
174. Explain `Intl.DateTimeFormat` and `Intl.NumberFormat`.
175. What is `Intl.Collator` for string comparison?

---

**Total: 175 Questions**

*Questions organized by difficulty level to help you prepare systematically for technical interviews at top tech companies.*
